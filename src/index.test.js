"use strict";

var expect = require('chai').expect;
var simpleDate = require('./simpleDate');

describe('simpleDate-func', function(){
  it('should work!', function(){
    expect(true).to.be.true;
  });
});
